---
description: Pull and run the next Ready build brief from the Notion Build Queue
---
Follow the "Starting a build session" procedure in CLAUDE.md. Pull the top
Status=Ready brief from the Notion Build Queue, set it Running, run the
pre-flight audit gate, then execute the brief end to end, then report back.
If the queue has no Ready row, say so and stop — never invent a build session.
