{
  "name": "@laude/extractor",
  "version": "0.0.1",
  "private": true,
  "description": "MOCK seeder standing in for the offline Python pipeline: writes realistic fake songs/lyrics/chords/performances/time-annotations into the Firebase emulator. No yt-dlp/Demucs/ML — the real Extractor replaces this behind the same data contract.",
  "type": "module",
  "scripts": {
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@laude/chords": "file:../../laudasist/packages/chords",
    "@laude/song-model": "file:../../laudasist/packages/song-model",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
