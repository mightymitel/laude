{
  "name": "@laude/local-library",
  "version": "0.0.1",
  "private": true,
  "description": "One local-library contract (DEC-70): local_songs, song_links, favorites, sync state — IndexedDB adapter for the browser now; Studio's SQLite retrofits behind the same interface later.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@laude/song-model": "*"
  },
  "scripts": {
    "test": "tsx --test src/local-library.test.ts"
  }
}
