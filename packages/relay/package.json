{
  "name": "@laude/relay",
  "version": "0.0.1",
  "private": true,
  "description": "The session relay as a MODULE inside Laudasist (DEC-52): authoritative in-memory session state, socket fast path + REST fallback. The cloud build mounts it in the single Express+socket.io App Hosting backend; the light build embeds it on the Studio machine (LAN mode) with no Firebase — the mirror and token verification are injected adapters.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@laude/session": "*",
    "@laude/song-model": "*",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsx --test src/state.test.ts src/relay.test.ts"
  }
}
