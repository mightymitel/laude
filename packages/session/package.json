{
  "name": "@laude/session",
  "version": "0.0.1",
  "private": true,
  "description": "Presenter-client SDK for the unified session: socket.io + REST against the stateful relay (no Firestore transport).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@laude/song-model": "*",
    "socket.io-client": "^4.8.3"
  },
  "scripts": {
    "test": "tsx --test src/session.test.ts"
  }
}
